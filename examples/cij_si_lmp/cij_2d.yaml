workdir: o.res_2d

backend: lammps

# which stiffness components to build (Voigt indices)
# 2D example (6 components): xx,yy,xy → 1,2,6
components: [1, 2, 6]

# strain magnitudes used for finite differences
strains: [0.00001, -0.00001]

# LAMMPS-specific settings
lammps:
  exe: lmp_mpi
  units: metal
  atom_style: atomic
  input_template: in.template
  data_file: in.data

common_files: [_potential.mod, _Si.sw]
common_path: '.'

# output
output:
  cij_json: cij.json
  units_cij: gpa
  symmetrize_cij: false
